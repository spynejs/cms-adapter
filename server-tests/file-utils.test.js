const test = require('node:test');
const assert = require('node:assert/strict');

test('should test file utils', () => {

  assert('should create data timestamp', () => {

    return true;

  });

});
